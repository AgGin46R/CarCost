<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Разрешения -->
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28"
        tools:ignore="ScopedStorage" />
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <!--
        REQUEST_INSTALL_PACKAGES удалено вместе с самообновлением: приложение
        больше не скачивает и не ставит APK само, обновления идут через магазин.
        RuStore относит это разрешение к чувствительным и требует обоснования,
        а обосновывать механизм, дублирующий магазин, нечем.
    -->

    <!-- Разрешения для карты и геолокации -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <!--
        ACCESS_BACKGROUND_LOCATION удалено: оно объявлялось, но НИ РАЗУ не
        запрашивалось в коде. Запись поездок идёт через сервис переднего плана
        с постоянным уведомлением, а ему это разрешение не требуется.

        RuStore относит его к чувствительным и просит обосновать каждое —
        обосновывать разрешение ради несуществующей функции нечем, и лишний
        повод для отказа при модерации не нужен.
    -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_LOCATION" />

    <!-- Объявление используемых функций оборудования -->
    <uses-feature
        android:name="android.hardware.camera"
        android:required="false" />
    <uses-feature
        android:name="android.hardware.location"
        android:required="false" />
    <uses-feature
        android:name="android.hardware.location.gps"
        android:required="false" />

    <application
        android:name=".App"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.CarCost"
        tools:targetApi="31">

        <!-- YANDEX MAPKIT API KEY (injected from local.properties via BuildConfig) -->
        <meta-data
            android:name="com.yandex.mapkit.ApiKey"
            android:value="${yandexMapKitKey}" />

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.CarCost.Splash"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <!-- Deep link for car invitations -->
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="carcost" android:host="invite" />
            </intent-filter>
        </activity>

        <!-- GPS Trip ForegroundService -->
        <service
            android:name=".data.gps.GpsTripService"
            android:foregroundServiceType="location"
            android:exported="false" />

        <!-- Navigation ForegroundService (Yandex Navigator) -->
        <service
            android:name=".data.navigation.NavigationService"
            android:foregroundServiceType="location"
            android:exported="false" />

        <!-- FCM: получает push-уведомления даже когда приложение закрыто -->
        <service
            android:name=".data.remote.fcm.CarCostFirebaseMessagingService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>

        <!--
            RuStore Push — второй транспорт, для устройств без сервисов Google.
            Доставка идёт через установленное приложение RuStore; если его нет,
            сервис просто не получит сообщений, и уведомления придут по Firebase.

            Мета-данные с каналом уведомлений SDK не задаём: сообщения у нас
            data-only, показывает их наш собственный код в PushMessageHandler,
            а не SDK.
        -->
        <service
            android:name=".data.remote.rustore.CarCostRuStoreMessagingService"
            android:exported="false">
            <intent-filter>
                <action android:name="ru.rustore.sdk.pushclient.MESSAGING_EVENT" />
            </intent-filter>
        </service>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

        <receiver
            android:name=".data.parking.ParkingTimerReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="com.aggin.carcost.PARKING_EXPIRED" />
            </intent-filter>
        </receiver>

        <!-- App Widget -->
        <receiver
            android:name=".presentation.widget.CarCostWidgetReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/appwidget_info" />
        </receiver>

    </application>

</manifest>